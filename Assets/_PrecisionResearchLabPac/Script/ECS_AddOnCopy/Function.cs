using UnityEngine;
using Unity.Collections;
using System;

public static class Function
{
    /// <summary> 効率が悪くはある </summary>
    // public static T[,] To2D<T>(NativeArray<T> src, int size) where T : struct
    // {
    //     var dst = new T[size, size];
    //     for (int i = 0; i < src.Length; i++)
    //         dst[i % size, i / size] = src[i];
    //     return dst;
    // }
}
